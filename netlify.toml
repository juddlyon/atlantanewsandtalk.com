[build]
  command = "node scripts/fetch-articles.mjs && (node scripts/summarize.mjs || echo 'Summarize failed, using existing digest') && npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[build.processing.html]
  pretty_urls = true
